package com.code.portfolio.project.domain;

/**
 * Classificacao de risco de um projeto. A ordem das constantes (do menor para o
 * maior risco) e usada para combinar os criterios de orcamento e prazo.
 */
public enum RiskLevel {
    BAIXO,
    MEDIO,
    ALTO
}
